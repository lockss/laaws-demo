## Prerequistes
You must have Docker and Docker Compose installed. Docker has excellent [installation guides](https://docs.docker.com/engine/installation/) for various platforms.

## Running The Demo
Running the following command will build (if necessary) and run the set of Docker containers that are necessary for a working demo of LAAWS components:

    docker-compose up
